# Ultimate-Typescript-Node-Docker-Setup

This is just a basic setup with only get route to get seeded data.

Main purpose is to outline nodejs (fastify) in typescript setup, docker, prism, and knex altogether.
